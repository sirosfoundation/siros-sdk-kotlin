<?xml version="1.0" encoding="utf-8"?>
<!--
    ISO 18013-5 §9.2.1 NFC static handover: the mdoc acts as an NFC Forum
    Type 4 Tag. android:requireDeviceUnlock="false" matches how a real mdoc
    presents (a locked phone can still be tapped to a reader for engagement;
    the actual credential data release still requires the app's own consent
    flow once a BLE session is established in a later phase).
-->
<host-apdu-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/nfc_hce_description"
    android:requireDeviceUnlock="false">
    <aid-group
        android:description="@string/nfc_hce_description"
        android:category="other">
        <!-- NFC Forum Type 4 Tag NDEF Application ID (fixed, per NFC Forum
             Type 4 Tag Technical Specification), not an ISO 18013-5-specific
             value. -->
        <aid-filter android:name="D2760000850101" />
    </aid-group>
</host-apdu-service>
